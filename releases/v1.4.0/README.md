# v1.4.0

- Symbols: TSLA (default), BTC, NVDA — switch with long-press
- Integer price and integer daily % change
- Official Phone (3) LED mask for pixel rendering
- Package: `com.juanito.teslagnlyph`

Install:

```bash
adb install -r glyph-stock-ticker-v1.4.0.apk
```